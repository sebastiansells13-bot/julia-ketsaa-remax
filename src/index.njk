---
layout: layouts/page.njk
title: Home
eleventyNavigation:
  key: Home
  order: 1
---
{% from "macros/listing-placeholder.njk" import listingPlaceholder %}
<div class="hero hero--photo">
  <img class="hero__bg" src="/img/hero/realty-hero.jpg" alt="" width="1920" height="900">
  <div class="hero__content wrapper">
    <h1>{{ business.name }}</h1>
    <p class="hero__title">{{ business.title }}</p>
    <p>{{ business.tagline }}</p>
  </div>
</div>

<section class="wrapper">
  <div class="section-heading">
    <h2>Featured Listings</h2>
    <a href="/listings/">View all listings →</a>
  </div>
</section>
<section class="listings-grid wrapper">
  {% for listing in listings.list | head(3) %}
  <a class="listing-card" href="/listings/{{ listing.address | slugify }}/">
    <div class="listing-card__media">
      {% if listing.image %}
      <img src="{{ listing.image }}" alt="">
      {% else %}
      {{ listingPlaceholder(loop.index0) }}
      {% endif %}
      <span class="listing-card__status listing-card__status--{{ listing.status | slugify }}">{{ listing.status }}</span>
      {% if listing.isSample %}<span class="listing-card__sample">Sample</span>{% endif %}
    </div>
    <div class="listing-card__body">
      <p class="listing-card__price">{{ listing.price }}</p>
      <h3>{{ listing.address }}</h3>
      <p class="listing-card__stats">{{ listing.beds }} bed · {{ listing.baths }} bath · {{ listing.sqft }} sqft</p>
    </div>
  </a>
  {% else %}
  <p>New listings are on the way — <a href="/contact/">reach out</a> for what's currently available.</p>
  {% endfor %}
</section>

<section class="services-grid wrapper">
  {% for service in services.list %}
  <div class="service-card">
    {% if service.icon %}<img src="{{ service.icon }}" alt="" class="service-card__icon">{% endif %}
    <h3>{{ service.title }}</h3>
    <p>{{ service.description }}</p>
  </div>
  {% endfor %}
</section>

{% if testimonials.list.length %}
<section class="testimonials wrapper">
  <h2>What Clients Say</h2>
  <div class="testimonials-grid">
    {% for t in testimonials.list %}
    <blockquote class="testimonial">
      <p>&ldquo;{{ t.quote }}&rdquo;</p>
      <cite>— {{ t.name }}</cite>
    </blockquote>
    {% endfor %}
  </div>
</section>
{% endif %}
